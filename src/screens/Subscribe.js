import React from 'react';
import {Text,View,StyleSheet} from 'react-native';
import Header from '../components/Header';

const Subscribe = () =>{
    return(
        <View style={{flex:1}}>
            <Header />
            <Text>hello from Subscribe</Text>

        </View>
    );
};

const styles = StyleSheet.create({

});

export default Subscribe;